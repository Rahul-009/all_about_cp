// DP on tree

// We have a rooted tree | we need to jump up
// find the k-th parent of a node | we jump k node and find the parent
	// Brute force O(N*Q)  | it is good if tree is balanced

// In binary lifting we jump in the power of 2's
	// we need to store 1, 2, 4, 8, 16, 32 ... in up matrix | space O(N*logN)
	// For each node we calculate up table | O(N*logN)
	// space bruteforce er cheye onek beshi lage 
	// space limited thakle go with bruteforce
	

// LCA - very useful (HLD)





