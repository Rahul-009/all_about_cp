

// divide tree in chains
// big chains == heavy chains | generally contains root
// small chains == light chains


// We find heavy chain using dfs | construct chains using DFS
// Then calculate  using segment tree on the chains


// We jump chain to chain until LCA is reached

