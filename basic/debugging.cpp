// dudh vaat check
	// long long check
	// precision check
	// capital , small letter check
	// new line , blank space check
	// *** vector / array restore korsi kina check
	// *** don't print INT_MAX and INT_MIN | they are dummy
	// bitwise shift use kore gun vaag korle bracket must | precision problem ase

// Problem with mod
	// onekgula number jog kore mod kora jay  
	// but number gula beshi boro hole shobgula alada alada mod kore nite hobe
	// negative number check +M korsi ?


// Graph
	// make sure to restore all the array
	// 2D grid er moves thik ase kina
	// INT_MAX naki LLONG_MAX

// Dynamic Programming
	// always keep base cas exact
		// if( n == 0 ); | don't do if(n<=0);
		// n < 0 jeno na hoy oijonno recursive call er age check boshano lagbe


// Segment Tree
	// function e return korso naki check koro
	// check neutral value

// X^1  | swapping between 0 and 1

