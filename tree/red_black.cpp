// Self Balanced BST

// maximum 2 rotation lagbe balanced binary te convert korte


	// roughly height balanced
	// searching is faster in AVL tree
	// insertion and deletion is faster in Red Black

// Every node is either Black or Red
// Root is always Black
// Every leaf which is Null is Black
// If node is red it's child must be black | two adjacent node can't be red
// two adjacent node can be black
// Every path from a node to it's descendant NULL node has same no. of black nodes


// If we color AVL tree it will be a red black tree
// But all red black trees are not AVL tree
// AVL trees are subset of Red black tree

// non leaf nodes must not have NULL nodes

// must check examples in khata/other sites
